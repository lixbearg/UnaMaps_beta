<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lixbearg.unamaps" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/unamaps_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MapaActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListaLocalidadesActivity"
            android:label="@string/title_activity_lista_localidades" >
        </activity>
        <activity
            android:name=".BetaHelpActivity"
            android:label="@string/title_activity_beta_help"
            android:theme="@style/Transparent">
        </activity>
    </application>

</manifest>
